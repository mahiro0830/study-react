import React from 'react';
import ReactDOM from 'react-dom/client';

import EventPoint from './03/EventPoint';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <EventPoint />
);
